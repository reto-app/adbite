{
  "name": "adbite-worker",
  "private": true,
  "type": "module",
  "description": "ffmpeg work for AdBite: creative transcodes and stitched reels.",
  "scripts": {
    "start": "node index.mjs",
    "once": "node index.mjs --once"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@supabase/supabase-js": "^2.116.0"
  }
}
