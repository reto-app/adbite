dist/
out/
.DS_Store

# The Test Shop's film. Ten megabytes of taco, regenerated by
# tools/cut-stage-clip.sh from the clips in dist/export/mexico-clips, which
# are not tracked either. The board.json beside it and the ad artwork are,
# so a clone has everything except the footage.
test-shop/media/
